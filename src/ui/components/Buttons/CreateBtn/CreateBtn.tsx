import React from 'react';
import { CreateIcon } from '@/ui/components';

const CreateBtn = () => {
    return (
        <button>
            <CreateIcon />
        </button>
    );
};

export default CreateBtn;
