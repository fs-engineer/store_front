import React from 'react';
import s from './footer.module.css';

const Footer = () => {
    return (
        <div className={s.footer}>
            <h1 className={s.title}>Footer</h1>
        </div>
    );
};

export default Footer;
