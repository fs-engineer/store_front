import React from 'react';
import { Container, Title } from '@/ui/components';

const Page = () => {
    return (
        <Container>
            <Title text={'Країни'} />
        </Container>
    );
};

export default Page;
