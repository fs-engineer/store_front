import React from 'react';
import AuthForm from '@/ui/components/auth/authForm/authForm';

const Page = () => {
    return (
        <div>
            <AuthForm />
        </div>
    );
};

export default Page;
